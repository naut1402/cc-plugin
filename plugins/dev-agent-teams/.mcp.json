{
  "mcpServers": {
    "dev-team-dashboard": {
      "command": "bun",
      "args": ["${user_config.dashboardApp}/mcp/server.ts"],
      "env": {
        "DEV_TEAM_DASHBOARD_HOME": "${user_config.dashboardHome}"
      }
    }
  }
}
