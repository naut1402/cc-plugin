{
  "name": "task-memory-silverbullet",
  "description": "Contract skills for reading and writing working memory via SilverBullet MCP",
  "version": "0.1.0",
  "author": {
    "name": "tttuan"
  },
  "license": "Apache-2.0",
  "skills": ["./skills/"],
  "userConfig": {
    "mcp_url": {
      "type": "string",
      "title": "SilverBullet MCP URL",
      "description": "URL of the silverbullet-mcp server",
      "default": "http://localhost:4000/mcp",
      "required": true
    },
    "mcp_token": {
      "type": "string",
      "title": "MCP Auth Token",
      "description": "Bearer token (MCP_SERVER_TOKEN from silverbullet-mcp)",
      "sensitive": true,
      "required": true
    }
  }
}
