{
  "mcpServers": {
    "dev-team-dashboard": {
      "command": "bun",
      "args": ["${DEV_TEAM_DASHBOARD_APP}/mcp/server.ts"],
      "env": {
        "DEV_TEAM_DASHBOARD_HOME": "${DEV_TEAM_DASHBOARD_HOME}"
      }
    }
  }
}
