{
  "name": "dev-agent-teams",
  "description": "Multi-agent development pipeline: investigate → design → implement → review → PR",
  "version": "0.0.1",
  "author": {
    "name": "tttuan"
  },
  "license": "Apache-2.0",
  "skills": ["./skills/"],
  "userConfig": {
    "dashboardApp": {
      "type": "string",
      "title": "Dashboard app directory",
      "description": "Absolute path to a local clone of agent-workflow (https://github.com/naut1402/agent-workflow). Used by /dev-dashboard and local runner-cli.",
      "required": true
    },
    "dashboardHome": {
      "type": "string",
      "title": "Dashboard registry home",
      "description": "Directory for projects.json and runners.json. Default: ~/.dev-team-dashboard"
    },
    "dashboardServerUrl": {
      "type": "string",
      "title": "Remote dashboard URL",
      "description": "Base URL of deployed dev-team-dashboard (e.g. https://dashboard.example.com). Required for --remote; handshake resolves projectId when omitted. Leave empty for local-only."
    },
    "dashboardApiToken": {
      "type": "string",
      "title": "Remote dashboard API token",
      "description": "Bearer token when remote dashboard requires auth.",
      "sensitive": true
    },
    "dashboardProjectId": {
      "type": "string",
      "title": "Default dashboard project id",
      "description": "Optional opaque project id. Prefer empty — resolve-remote.mjs resolves from name or git remote."
    },
    "dashboardProjectName": {
      "type": "string",
      "title": "Default dashboard project name",
      "description": "Optional UI display name of the remote project when id is unknown."
    }
  }
}
